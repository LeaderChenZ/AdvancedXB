package com.dfbz.dao;

/**
 * @author Chen
 * @description
 * @date 2019/12/7 16
 */
public class UserProvider {

    /*public String selectFocus(long uid){
        StringBuilder sb = new StringBuilder();
        sb.append("select u.* from " +
                "`user` u,userfocus  uf " +
                "where " +
                "u.id = uf.user_focus_id " +
                "and " +
                "uf.user_id = #{uid}");
        return sb.toString();
    }*/
}
